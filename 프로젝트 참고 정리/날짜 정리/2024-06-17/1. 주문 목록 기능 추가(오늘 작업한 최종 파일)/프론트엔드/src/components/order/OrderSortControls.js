// OrderSortControls.js
/*
import React from 'react';

// 정렬 컨트롤 컴포넌트
function OrderSortControls({ handleSortChange, sortDirection, setSortDirection }) {
  return (
    <div className="order-sort-controls-container">
      <label>정렬:</label>
      <select value={sortDirection} onChange={(e) => setSortDirection(e.target.value)} className="order-sort-controls-select">
        <option value="asc">오름차순</option>
        <option value="desc">내림차순</option>
      </select>
      <select onChange={(e) => handleSortChange(e.target.value)} className="order-sort-controls-select">
        <option value="default">기본</option>
        <option value="date">날짜</option>
        <option value="totalPrice">총 가격</option>
        <option value="status">상태</option>
      </select>
    </div>
  );
}

export default OrderSortControls;
*/